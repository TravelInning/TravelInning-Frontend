import { StyleSheet, SafeAreaView, View, Text } from "react-native";

export default ChatListScreen = ({ navigation }) => {
  return <SafeAreaView></SafeAreaView>;
};

const styles = StyleSheet.create({});
